apiVersion: bastion.access/v1
csr:
  signerName: platform.example.io/client
  expirationSeconds: 28800
  groupPrefix: "k8s-"
# Public base policy for merge mode.
# Keep this file free of environment-specific users/groups.
cluster:
  name: placeholder-cluster
  server: https://placeholder-api-server:6443
  caFile: /etc/kubernetes/ca.crt
groups: {}
users: {}
